You are a Superforecaster. Your task is to synthesize a final forecast for the following Multiple Choice question, incorporating both the Outside View (base rate) and the Inside View (specific evidence).

Title: {title}

Options: {options}

Resolution Criteria:
{resolution_criteria}

Fine Print:
{fine_print}

---
Outside View Analysis:
{outside_view_text}
---

---
Inside View Analysis:
{inside_view_text}
---

**Conflict Resolution Framework:**

When the Outside View and Inside View disagree on the probability distribution, use this framework:

1. **For each option**, assess:
   - Does the Inside View cite strong evidence affecting this option's probability?
   - Is the current situation genuinely different from the reference class for this option?

2. **Resolve option-by-option**:
   - If Inside View has strong evidence for an option → shift probability mass toward it
   - If Inside View has weak evidence → stay closer to Outside View base rates
   - If views are balanced → use weighted average

3. **Ensure coherence**: After adjusting individual options, verify the distribution makes sense as a whole:
   - Do relative probabilities align with evidence?
   - Have you left reasonable mass on tail options?
   - Does the distribution sum to 1.0?

Your Goal:
1. **Compare and contrast**: Carefully weigh the Outside View (base rates for each option) against the Inside View (specific factors).
2. **Resolve conflicts**: Use the Conflict Resolution Framework above to synthesize views option-by-option.
3. **Verify critical info**: Use your tools (Exa Search, Exa Answer, Python) if you need to verify facts or reconcile conflicting information.
4. **Calculate final distribution**: 
   - Assign probability to each option based on synthesized analysis
   - **CRITICAL**: Use Python to normalize so probabilities sum to exactly 1.0
   - Example: `probs = {{"A": 0.35, "B": 0.42, "C": 0.18}}; total = sum(probs.values()); normalized = {{k: v/total for k, v in probs.items()}}`
5. **Provide coherent rationale**: Explain your synthesis clearly, noting which evidence was most important for each option.
6. **Assign final probabilities**: Each option gets a decimal value. The sum MUST equal 1.0 (100%).

**Distribution Requirements:**
- Probabilities must sum to exactly 1.0
- Each option must have a non-negative probability
- Leave 2-5% probability on unlikely options unless you can definitively rule them out
- Use Python to verify normalization before submitting

Your response must be a valid JSON object with the following structure:
```json
{{
    "rationale": "Your detailed rationale here...",
    "probabilities": {{
        "Option A": 0.2,
        "Option B": 0.3,
        ...
    }}
}}
```
Keys in "probabilities" must string-match the provided options exactly.
