name: Forecast on new AI tournament questions (cron-jobs.org)

on:
  workflow_dispatch:

# Add concurrency group to prevent parallel runs
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false


# Daily job to run the simple forecast bot
jobs:
  forecast_job:
    runs-on: ubuntu-latest # determines the machine that will run the job - keep as is
    steps: # sets up the steps that will be run in order
      # setup repository with all necessary dependencies - keep as is
      - name: Check out repository
        uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Install poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true
      # Adding the below will make the workflow faster, but will mean you don't automatically get updates from forecasting-tools and other packages
      # - name: Load cached venv
      #   id: cached-poetry-dependencies
      #   uses: actions/cache@v4
      #   with:
      #     path: .venv
      #     key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}
      - name: Install dependencies
        run: poetry install --no-interaction --no-root
      - name: Run bot
        run: |
          poetry run python main.py --mode spring-aib-2026 --submit --num-runs 5 --skip-prev --get-prediction-market --log-to-supabase --no-ask-news
        # this reads the environment variables from the github repository.
        # Store under Settings --> Secrets and variables --> Actions
        env:
          METACULUS_TOKEN: ${{ secrets.METACULUS_TOKEN }} # replace this with the name of the variable under which you stored your own Metaculus token
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          ASKNEWS_CLIENT_ID: ${{ secrets.ASKNEWS_CLIENT_ID }}
          ASKNEWS_SECRET: ${{ secrets.ASKNEWS_SECRET }}
          EXA_API_KEY: ${{ secrets.EXA_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          
          # Model Configuration
          OUTSIDE_VIEW_MODEL: "gpt-5-mini"
          INSIDE_VIEW_MODEL: "gpt-5-mini"
          SUMMARY_MODEL: "gpt-5-nano"
          FINAL_FORECAST_MODEL: "gpt-5.2"
          FINAL_FORECAST_USE_AGENT: "false"
          # Logging configuration
          LOG_ENABLE: "true"
          LOG_PLAINTEXT: "true"
          LOG_INCLUDE_COMMENT: "true"
          LOG_GLOBAL_STREAM: "true"
          RUN_ID: "github-run-${{ github.run_number }}"
      
      - name: Upload forecast logs
        uses: actions/upload-artifact@v4
        if: always() # Upload logs even if bot fails
        with:
          name: forecast-logs-run-${{ github.run_number }}
          path: logs/
          retention-days: 30